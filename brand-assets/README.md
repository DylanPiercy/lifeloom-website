# Local brand assets

Put the real brand files in this folder. Files here are ignored by Git, except this README.

The default local config expects:

- `lifeloom-mark.svg`
- `rivalry-mark.svg`

Run `npm run prepare` to copy them into the generated `public/runtime/brand/` directory before previewing or deploying.
